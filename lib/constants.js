const chalk = require("chalk");

const TIMEOUT = 10000;
const SEPARATOR = chalk.grey("\n-----------------------------------------------------------------\n");

module.exports = {
  TIMEOUT,
  SEPARATOR,
};
